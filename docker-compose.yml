services:
  netmon:
    build: .
    container_name: netmon
    ports:
      - "514:514/udp"
    volumes:
      - netmon-data:/data
      - ./config.yaml:/app/config.yaml:ro
      - ./pfsense-config.xml:/config/pfsense.xml:ro
    restart: unless-stopped

volumes:
  netmon-data:
